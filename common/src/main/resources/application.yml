server:
  port: 8089
# Eureka Server 地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8090/eureka/
  # eureka实例ID
  instance:
    instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${spring.application.instance_id:${server.port}}
spring:
  application:
    name: common
  datasource:
    url: jdbc:postgresql://120.25.167.218:5432/postgres
    username: postgres
    password: postgres

ribbon:
  ReadTimeout: 120000
  ConnectTimeout: 30000
demo:
  gn:
    ceShi : 11